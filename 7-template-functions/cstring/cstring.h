#include <iostream>
#include <cstring>

size_t Strlen(const char* str);

int Strncmp(const char* first, const char* second, size_t count);

std::strcpy(char* dest, char* res);

std::strncpy(char* dest, char* res, size_t count);  // <= count символов
